{
  "name": "TradeSphere",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "npm install --prefix backend && npm install --prefix frontend && npm install --prefix dashboard && concurrently \"npm start --prefix backend\" \"npm start --prefix frontend\" \"npm start --prefix dashboard\""
  },
  "devDependencies": {
    "concurrently": "^8.0.1"
  }
}
